pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // PERBAIKAN: Tambahkan baris ini untuk memberitahu Gradle
        // agar mencari library di repositori JitPack.
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Concentrack"
include ':app'
